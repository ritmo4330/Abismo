# 雪墅章节线索映射（Task 3.4 分阶段）

## 范围

- 本批次仅覆盖雪墅章节（chapter_id = "2"）。
- 资源目录：`assets/objects/clues/ch2_snow_villa/`
- 资源文件命名：与线索 id 一致（`<id>.tres`）。

## 规则对齐

- 本批次所有线索均为 `scene_evidence`，`is_conclusion = false`。
- 特殊标明“不高亮”的深入调查线索已设置 `highlight_visible = false`。
- 父子线索映射按“父写 child_clue_ids，子写 parent_clue_id”落地。

## 统计

- 基础线索（含搜身）：32 条
- 深入调查子线索：6 条
- 合计资源文件：38 条

## 父子线索映射

- `2_lin_1` -> `2_lin_1_1`（深入调查 1，不高亮）
- `2_lin_5` -> `2_lin_5_2`（深入调查 2，不高亮）
- `2_mei_2` -> `2_mei_2_5`（深入调查 5）
- `2_study_5` -> `2_study_5_24`（深入调查 24）
- `2_mu_0` -> `2_mu_0_27`（深入调查 27，不高亮）
- `2_lin_0` -> `2_lin_0_25`（深入调查 25，不高亮）

## 章节边界说明

- `线索.txt` 中“①'（信纸）”对应 id `3_lin_1`，属于梦墅章节，不在本批次（雪墅）范围内，将在后续章节映射时处理。
