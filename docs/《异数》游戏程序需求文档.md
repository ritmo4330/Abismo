# 《异数》游戏程序需求文档

> **[开发者注]** 
> 本文档是游戏的需求总纲。针对 AI 辅助开发，请务必结合本项目根目录下独立的**技术架构指导文档（如 TDD 或 AI 开发指南）**进行代码编写。核心架构基于“EventBus行为解耦”、“DataManager数据单例”及“单向数据流”原则。

## 1. 游戏概述

《异数》是一款改编自同名剧本杀的推理AVG游戏，英文名为"Abismo"。玩家将扮演剧本中的角色，通过探索环境、收集线索、与其他角色互动，逐步揭开故事的真相。游戏将结合视觉小说和冒险解谜元素，提供沉浸式的游戏体验。项目策划案见[异数改编项目策划案](https://qcngzcz6iesn.feishu.cn/wiki/D8zywNRQ7iH9vckmGVeclYKInGe)。

## 2. 主要功能需求

- 使用 Godot 4.6.1 版本开发
- 操作系统： Windows 10 及以上
- 游戏类型：单人推理AVG游戏，画面风格为2D像素俯视角
- 游戏分辨率：1920x1080

文件结构：

```text
Abismo/                    # 项目根目录
├── addons/                # 插件文件夹（包含 dialogic 等第三方插件）
├── assets/                # 游戏美术及音频资源文件夹
│   ├── characters/        # 角色相关资源
│   ├── game/              # 游戏通用基础资源
│   ├── interior/          # 室内场景美术资源
│   ├── objects/           # 交互物件及道具资源
│   └── UI/                # 用户界面贴图及字体资源
├── scenes/                # 游戏预制体与场景文件夹 (.tscn)
│   ├── characters/        # 角色场景实体预制体
│   ├── objects/           # 物件及可交互实体预制体
│   ├── rooms/             # 游戏关卡与房间地图场景
│   ├── test/              # 效果测试用场景
│   └── UI/                # UI 组件及系统菜单预制体
├── scripts/               # 游戏逻辑脚本文件夹 (.gd)
│   ├── characters/        # 角色行为控制器与状态机代码
│   ├── objects/           # 物件逻辑与交互事件代码
│   ├── scenes/            # 场景管理器与流程控制代码
│   └── UI/                # 系统界面和弹窗逻辑代码
├── Tilesets/              # 瓦片集地图配置文件夹 (.tres)
├── 素材库/                # 原始素材库（存放未处理的外部收集资源与参考图）
└── project.godot          # Godot 项目核心配置文件
```



### 2.1 角色操控

- 玩家可以通过键盘或摇杆控制角色在场景中移动：
  - 使用WASD键或左摇杆控制角色的前后左右移动
  - 角色移动时应有流畅的动画效果
  - 角色移动时应有适当的音效反馈
- 角色移动过程中可以触发特定事件或与环境互动

采用状态机（State Machine）来管理角色的移动状态，包括但不限于：站立、行走、跑步等。每个状态应有对应的动画和音效，并且在状态切换时应有平滑的过渡效果。

相关文件：

- 角色状态机脚本文件夹：`scripts/characters/player/state_machine`
  - 包含`node_state_machine.gd`（状态机核心逻辑）、`idle_state.gd`（站立状态）、`walk_state.gd`（行走状态）
- 角色脚本（疑似无用）：`scripts/characters/player/player.gd`
- 角色状态脚本：`scripts/characters/player/player_state.gd`，包含角色状态的定义和管理逻辑
- 角色动画资源：`assets/characters`，包含角色的不同状态对应的动画文件

### 2.2 场景交互

- 玩家可以与场景中的物品进行交互:
  - 使用F键或手柄A键进行交互
  - 交互对象包括但不限于：门、各类线索物品、其他角色等
  - 交互时应有相应的动画和音效反馈
- 交互对象可以触发不同的事件，如打开门、收集线索、与角色对话等
- 交互对象应有明显的视觉提示，帮助玩家识别可交互的元素
- 交互对象应有适当的碰撞检测，确保玩家只能在合理范围内进行交互
- 交互对象的状态应根据游戏进程动态变化，例如某些物品在特定条件下才可交互

相关文件：

- 交互对象脚本文件夹：`scripts/objects/interactable`
  - 目前仅有`item_data.gd`（物品数据定义）、`pickable_item.gd`（可拾取物品逻辑）
  - 考虑重构为`scripts/interactable/interactable.gd`（交互对象核心逻辑），并基于核心逻辑构建不同交互对象的具体逻辑如`door.gd`（门的交互逻辑）、`clue.gd`（线索物品的交互逻辑）等
- 当前玩家状态脚本：`scripts/characters/player/player_state.gd`，仍包含玩家拾起物品的逻辑，应将其拆分到交互对象脚本中，以实现更清晰的职责分离
- 当前仍存在的场景相关脚本：
  - `scripts/scenes/door.gd`（经过门时的场景切换逻辑）
  - `scripts/scenes/room.gd`（房间场景的管理逻辑）
  - `scripts/scenes/transition.gd`（场景过渡动画的管理逻辑）
  - 考虑将这些场景相关的逻辑重构为一个统一的场景管理器脚本，例如`scripts/scenes/scene_manager.gd`，负责管理场景切换、过渡动画以及与交互对象的协作等功能

#### 2.2.1 线索收集

- 线索物品将作为可收集的物品存在于场景中，玩家可以通过交互进行收集，称为一次*调查*：
  - 使用F键进行调查
  - 设计一个线索记录栏，当玩家收集到线索时，该线索的简介、具体条目、发现位置等自动记录到线索记录中，并按照*发现位置*进行分类，每一个分类下按照*发现时间*进行排序
  - 线索应有标题、详细的描述信息，是推理的重要依据
  - 可设计新增线索的红点提示
- 场景中的线索物品应有适当的视觉提示，例如高亮显示，帮助玩家识别线索：
  - 大部分值得收集的线索在地图上以高亮显示，少数线索可能需要其他线索或结论已知才有意义，在此前不高亮显示，但也可以直接调查收集。对于一些关键性线索（例如能直接锁定凶手的线索），不予高光显示
  - 对于一般线索，玩家可以通过交互收集，收集后视觉提示消失，玩家无法再次调查该物品
  - 对于能够进行深入调查的线索物品，在一次调查完后，仍保留视觉提示，玩家可以再次调查该物品获得更多的线索
  - 但对于某些特殊的可深入调查的线索，在被调查一次后，将取消该物品的视觉提示，玩家可能需要满足特定条件才能再次调查
- 某些对话可以作为线索被记录到线索记录栏中，例如角色的口供，口供可能由角色主动提供，也可能需要通过询问得知（可能有些话题不适合在公聊时谈论，此时需要和对方私聊）
- 线索记录栏应支持搜索和筛选功能，帮助玩家快速找到相关线索
- 线索记录栏应支持保存和加载功能，允许玩家在不同时间点继续游戏

相关文件：
暂无，待设计

### 2.3 对话系统

使用 Dialogic 2 插件实现对话系统，满足以下需求：

- 玩家可以与其他角色进行对话：
  - 使用F键触发对话
  - 对话界面应清晰易读，包含角色头像、名字和对话文本
  - 对话过程中应有适当的动画和音效反馈
  - 对话可能会有多个选项，可通过鼠标左键点击选择
- 对话选项应根据玩家的选择动态变化，影响游戏进程和结局
- 对话系统应支持保存和加载功能，允许玩家在不同时间点继续游戏
- 对话系统暂不支持多语言版本

相关文件：

- Dialogic 2 插件相关文件夹：`addons/dialogic`，包含对话系统的核心逻辑和资源
- 对话数据文件夹：`assets/dialogues/*.dtl`，为对话系统使用的对话数据文件，包含对话文本、选项、分支逻辑等信息
- 插件所用的角色资源文件夹：`assets/characters/npcs/*.dch`，为对话系统中使用的角色资源文件，包含角色的头像、名字等信息

### 2.4 推理系统

- 推理系统是游戏的核心功能之一，玩家通过收集的线索进行推理，逐步揭开故事的真相：
  - 推理界面应清晰易用，允许玩家查看已收集的线索和相关信息
  - 设计一个疑点记录栏，当新的疑点出现时自动记录到疑点记录中，按照提出时间进行排序。可设计新增疑点的红点提示
- 玩家可以随时在疑点记录栏中选择疑点进行推理：
  - 记录栏界面有标题、详细的描述信息，以及交互按钮
  - 玩家可鼠标左键点击按钮从已知线索中选取线索组成逻辑链或形成关联，即可得出结论，结论同样记录在线索记录栏中（可以作为线索被选取），并加上“结论”标签，同时疑点记录栏中在对应疑点上添加“已解决”标签，并补充结论条目
- 推理过程中应有适当的提示和引导，帮助玩家理解线索之间的关系（待补充）
- 推理过程中应有适当的动画和音效反馈（待补充）

相关文件：
暂无，待设计

### 2.5 好感度系统

- 每名角色内置一个好感度系统，用以反映其对主角的信任或友好程度，依据数值分为三个阶段：疏远、中立、信任：
  - 好感度为0~100的整数，其中0~29为疏远，30~70为中立，71~100为信任
- 不同角色对主角的初始好感度略有差异，但均落在中立区间：
  - 穆暌信初始好感度40，松林夕初始好感度60，苏溟玖初始好感度50，乌停湘初始好感度55，夏中清初始好感度45
- 当主角与其他角色谈论某些话题时，角色会根据好感度阶段给予不同反应
- 当角色即将做出关键抉择时，好感度阶段同样会对其抉择产生影响
- 暂未决定好感度变化是否向玩家公开

相关文件：
暂无，待设计

### 2.6 存档系统

- 游戏应支持存档和读档功能，允许玩家在不同时间点继续游戏：
  - 玩家可以在游戏中任意时刻进行存档，存档时应保存当前的游戏状态，包括角色位置、已收集的线索、已发现的疑点、对话进度、好感度等信息
  - 玩家可以通过主菜单或游戏内界面加载之前的存档，继续游戏
  
相关文件：
暂无，待设计；Dialogic 2 插件可能提供部分存档功能，后续可以注意一下

## 3. 数据定义

### 3.1 玩家数据

玩家数据包括但不限于以下内容：

- 角色名称
- 角色性别
- 角色位置
- 已收集的线索列表
- 已发现的疑点列表
- 对话进度（待考虑，因为Dialogic 2插件可能提供存档功能）
- 好感度数据
- 其他相关数据待补充

相关脚本位置：可设计`scripts/players/player_data.gd`代替当前的`scripts/characters/player/player_state.gd`，专门用于定义和管理玩家数据结构和相关操作，例如保存、加载、更新等功能（待考虑）

## 4. 游戏流程

游戏总共分为五个章节，每个章节包含多个场景，玩家需要在每个场景中完成特定的任务和目标，逐步推进故事的发展

旁白可作为引导玩家进入游戏世界的工具，在游戏流程中穿插出现，提供背景信息、提示和引导，增强游戏的沉浸感和故事性。旁白的内容应与游戏剧情紧密结合，帮助玩家理解故事的发展和角色的动机，同时也可以为玩家提供一些线索和提示，帮助他们更好地进行推理和解谜。

旁白将以文本形式出现，配合适当的动画和音效效果，增强游戏的氛围和情感表达。

### 4.1 第一章 悬念：神秘意象的闪回

- 旁白：你醒了？你已经是个女孩子了——开个玩笑，你还有自己选择的权利。（此时玩家选择主角性别。性别对剧情不产生影响，但可能影响某些交互逻辑）
- 旁白：你还记得你是谁吗？（此时玩家设置主角名称）
- 旁白：很好，看来一切正常。接下来，来听故事吧。（以下进入开场动画，BGM：《调律》）......
- 旁白：好了，故事到这里就讲完了。晚安，祝你做个好梦。

### 4.2 第二章 幕启：初入雪墅的圜局

#### 4.2.1 场景一：入局（BGM：《冬之旋律》）

未完待续