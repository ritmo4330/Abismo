# 流程编排设计文档

本文档用于指导后续把第一章“雪墅”完整落地。当前方案采用务实路线：保留现有 NPC 自由交互对话结构，只新增一层轻量流程脚本，用来处理场景转换触发的自动对话、章节节点推进和必要的场景动作。

---

## 1. 核心判断

第一章中的对话可以分为两类。

| 类型 | 触发方式 | 推荐归属 |
|---|---|---|
| 自由交互对话 | 玩家自由行动，靠近 NPC 按交互键触发 | `NpcDialogue.timeline_name` + Dialogic 变量 |
| 流程自动对话 | 场景转换后、剧情节点到达后自动触发 | 新增 `FlowManager` 控制 |

当前项目中，NPC 自由交互对话已经基本按这种方式在做，例如 `1_1_zhou.dtl`、`1_1_wu.dtl` 这类 timeline 内部通过 Dialogic 变量控制选项显隐、重复内容和新增内容。这个方向可以保留。

短期不建议强行引入完整 `DialogueRouteData` 路由系统，也不建议马上把 `NpcDialogue.timeline_name` 改成 `npc_id + entry_id`。那会增加额外配置层，而当前真正缺的是“流程自动对话”和“场景切换后的动作编排”。

---

## 2. 职责边界

### 2.1 现有模块继续保留

| 模块 | 职责 |
|---|---|
| `DataManager` | 运行时数据中心：线索、疑点、好感度、world flags、后续可加入当前流程节点 |
| `SceneManager` | 场景切换、房间加载、玩家常驻、出生点落位、过场动画 |
| `DialogueManager` | 接收对话请求、启动 Dialogic、处理对话结束 |
| `NpcDialogue` | 自由交互 NPC 的被动入口，保留 `timeline_name`，新增稳定 `npc_id` |
| `EventBus` | 跨系统信号，不保存状态 |

### 2.2 新增 FlowManager

建议新增：

```text
scripts/flow/flow_manager.gd
```

并注册为 Autoload：

```text
FlowManager="*res://scripts/flow/flow_manager.gd"
```

`FlowManager` 只处理流程编排：

- 当前章节、当前流程节点。
- 场景切换后是否自动播放某段 timeline。
- 场景加载后动态实例化当前流程需要的 NPC。
- 根据当前大章节/流程节点为 NPC 赋值自由交互 `timeline_name`。
- Dialogic 发出的流程信号。
- 需要时从 `Dialogic.VAR` 读取或写入对话变量。
- 私聊这种“对话 -> 切场景 -> 自动对话 -> 返回场景”的流程。
- 必要时设置 `DataManager.world_flags` 或 Dialogic 变量。

`FlowManager` 不处理：

- 不替代 Dialogic 的选项分支。
- 不替代 NPC 的自由交互 timeline。
- 不直接维护线索、疑点、好感度细节。
- 不直接做地图加载细节。

---

## 3. 自由交互对话方案

### 3.1 保留当前 timeline 挂载方式

玩家自由行动时，与 NPC 交互仍然走当前流程：

```text
Player InteractionArea
-> NpcDialogue.interact()
-> EventBus.dialogue_requested.emit(timeline_name)
-> DialogueManager
-> Dialogic.start(timeline_name)
```

`NpcDialogue` 需要保留 `timeline_name`，并新增 `npc_id`：

```gdscript
@export var npc_id: String = ""
@export var npc_name: String = ""
@export var timeline_name: String = ""
```

交互逻辑仍然只做一件事：如果 `timeline_name` 有值，就请求播放该 timeline。NPC 自己不判断当前章节，也不负责切换 timeline。

第一章大厅中：

- 大厅中的周崇安继续挂 `1_1_zhou`。
- 乌停湘继续挂 `1_1_wu`。
- 钟歧继续挂 `1_1_zhong`。
- 管家继续挂 `1_1_butler`。

同一个 timeline 内部通过 Dialogic 变量判断：

- 是否已经自我介绍过。
- 哪些选项已经点过。
- 搜身选项是否开启。
- 私聊选项是否开启。
- 某些追加话题是否出现。
- 某些一次性选项是否隐藏。

这类逻辑适合放在 Dialogic 中，因为它本质是对话文本和选项状态。

### 3.2 大章节切换时如何配置自由交互 timeline

自由交互 NPC 的 `timeline_name` 由 `FlowManager` 在实例化 NPC 时统一赋值。不要让 NPC 自己判断章节。

推荐做一个简单映射：

```gdscript
const FREE_INTERACTION_TIMELINES: Dictionary = {
    "ch1_snow_villa": {
        "butler": "1_1_butler",
        "zhou": "1_1_zhou",
        "mu": "1_1_mu",
        "lin": "1_1_lin",
        "wu": "1_1_wu",
        "zhong": "1_1_zhong",
    },
    "ch2_dream_villa": {
        "butler": "2_1_butler",
        "zhou": "2_1_zhou",
        "mu": "2_1_mu",
        "lin": "2_1_lin",
        "wu": "2_1_wu",
        "zhong": "2_1_zhong",
    },
}
```

`FlowManager` 实例化 NPC 后：

```gdscript
func apply_free_timeline(npc: NpcDialogue) -> void:
    var chapter_routes: Dictionary = FREE_INTERACTION_TIMELINES.get(current_chapter_id, {})
    if not chapter_routes.has(npc.npc_id):
        return
    npc.timeline_name = String(chapter_routes[npc.npc_id])
```

如果同一大章节内不同流程节点需要 NPC 挂不同自由交互 timeline，可以把 key 扩展为 `chapter_id + step_id`：

```gdscript
const FREE_INTERACTION_TIMELINES_BY_STEP: Dictionary = {
    "ch1_snow_villa/ch1_1_intro_hall": {
        "zhou": "1_1_zhou",
    },
    "ch1_snow_villa/ch1_4_private_chat": {
        "zhou": "1_1_zhou",
    },
}
```

第一章暂时可以按章节映射，因为 `1_1_xxx` 已经通过 Dialogic 变量覆盖介绍、搜身、私聊等追加内容。

### 3.3 Dialogic 变量适用范围

适合继续放在 Dialogic 变量里：

- `Ch1.NPCIntro.Zhou.Selected1`
- `Ch1.BodySearch.Wu.FirstDone`
- `Ch1.PrivateChat.Enabled`
- `Ch1.PrivateChat.Lin.Asked.Reason`
- `Ch1.PrivateChat.Zhou.Removed.Tools`

这类状态主要服务于“对话内部如何展示”。

不建议只放在 Dialogic 变量里：

- 当前流程节点。
- 下一次场景切换后要自动播放哪段 timeline。
- 当前私聊对象。
- 是否正在等待对话结束后切场景。
- 存档恢复时需要直接影响场景加载的状态。

这些更适合由 `FlowManager` 或 `DataManager` 管理。

---

## 4. 自动流程对话方案

### 4.1 自动对话的定义

自动对话指不是玩家主动点击 NPC 触发，而是流程到达某个节点后自动播放的 timeline。

第一章中典型例子：

- 管家带玩家到二楼后自动说明下一步，并在进入案发房间后自动说明调查规则。
- 1.3 回到大厅后，自动播放多人推理讨论。
- 选择私聊后，切到会客厅并自动播放 `1_4_{角色}`。
- 进入第二轮搜证后，自动播放 `1_5_all`。
- 玩家准备离开研究所时，自动播放 `1_5_exit` 或 `1_5_zhong`。

这些内容应该由 `FlowManager` 控制。

### 4.2 FlowManager 建议字段

第一版保持简单：

```gdscript
extends Node

var current_chapter_id: String = "ch1_snow_villa"
var current_step_id: String = "ch1_1_intro_hall"
var current_room_id: String = ""
var pending_auto_timeline: String = ""
var private_chat_target: String = ""
var _pending_flow_signal: String = ""
var _pending_action_after_dialogue: String = ""
var _current_room: Node2D = null
var _npc_locations: Dictionary = {}
```

说明：

- `current_step_id` 记录当前流程节点。
- `pending_auto_timeline` 用于场景加载完成后自动播放。
- `private_chat_target` 用于处理“进入会客厅后播放谁的私聊”。
- `_pending_flow_signal` 用于延迟执行对话结束后的流程动作。
- `_pending_action_after_dialogue` 用于明确记录“timeline 结束后再执行”的动作，避免在 Dialogic 还没收尾时切场景。
- `_current_room` 用于在 `room_loaded` 后清理和挂载动态 NPC。
- `_npc_locations` 是当前全局 NPC 位置表。每个 `npc_id` 同一时间只允许登记一个 `room_id + spawn`，避免同一个角色在多个房间重复出现。

### 4.3 FlowManager 建议接口

```gdscript
func set_step(step_id: String) -> void
func request_scene_change(target_scene_path: String, spawn_point: String, auto_timeline: String = "") -> void
func on_room_loaded(room: Node2D, room_id: String) -> void
func on_room_presented(room: Node2D, room_id: String) -> void
func play_pending_auto_timeline() -> void
func handle_flow_signal(signal_name: String) -> void
func handle_dialogue_ended() -> void
func get_dialogic_var(path: String, default_value: Variant = null) -> Variant
func set_dialogic_var(path: String, value: Variant) -> void
func set_npc_location(npc_id: String, room_id: String, spawn_name: String, timeline_name: String = "", scene_path: String = "") -> void
func setup_room_actors(room: Node2D) -> void
func spawn_npc(spawn_data: Dictionary, room: Node2D) -> void
func apply_free_timeline(npc: NpcDialogue) -> void
```

第一版不需要复杂资源表。可以直接用 `match signal_name` 写清楚第一章流程，后续内容变多后再抽数据。

读取 Dialogic 变量时要注意：Dialogic 变量只有在 Dialogic Autoload ready 后才可用。`FlowManager` 作为 Autoload 可以连接信号，但实际读取变量应放在 `_ready()` 之后的流程回调里，不要在脚本成员初始化阶段访问 `Dialogic.VAR`。

---

## 5. EventBus 信号补充

建议新增：

```gdscript
signal flow_signal_requested(signal_name: String)
signal room_loaded(room: Node2D, room_id: String)
signal room_presented(room: Node2D, room_id: String)
signal dialogue_finished(timeline_name: String)
```

用途：

- `flow_signal_requested`：Dialogic signal 转发给 `FlowManager`。
- `room_loaded`：`SceneManager` 在黑幕下完成房间加载后通知 `FlowManager`。
- `room_presented`：`SceneManager` 在淡入完成后通知 `FlowManager`。
- `dialogue_finished`：`DialogueManager` 在 timeline 结束时通知 `FlowManager`。

如果暂时不想加 `dialogue_finished`，也可以让 `DialogueManager._on_dialogue_ended()` 直接调用 `FlowManager.handle_dialogue_ended()`。但从架构上看，走 `EventBus` 更统一。

---

## 6. Dialogic 与 FlowManager 通信约定

核心规则：

```text
Dialogic timeline 负责声明“发生了什么”
FlowManager 负责决定“接下来做什么”
```

timeline 中不要直接切场景，不要直接实例化 NPC，也不要直接控制玩家。timeline 只做三件事：

- 设置对话变量。
- 记录线索/疑点。
- 发出 `flow:*` 信号。

### 6.1 通过 signal_event 通知流程

Dialogic timeline 使用 Signal 事件：

```text
[signal arg="flow:start_initial_search"]
[signal arg="flow:enter_private_chat"]
[signal arg="flow:exit_private_chat"]
```

外部连接 `Dialogic.signal_event`：

```gdscript
func _ready() -> void:
    Dialogic.signal_event.connect(_on_dialogic_signal)

func _on_dialogic_signal(argument: String) -> void:
    if not argument.begins_with("flow:"):
        return
    EventBus.flow_signal_requested.emit(argument.trim_prefix("flow:"))
```

### 6.2 text_signal 不作为主流程入口

Dialogic 的 `[signal=xxx]` 文本效果也能发 `Dialogic.text_signal`。它适合做文本播放到某个字时的表现，例如音效、镜头轻微反馈、UI 提示。

主流程推进统一使用 `signal_event`，避免流程动作被文字播放速度、跳字、跳过文本等表现细节影响。

### 6.3 variable_changed 不作为主流程入口

`Dialogic.VAR.variable_changed` 可以监听变量变化，但不建议作为章节流程的主驱动。

原因：

- 变量变化频率高，信号会很吵。
- 很难从“变量变了”判断“是否应该切场景”。
- 后续调试时不如显式 `flow:*` 信号清楚。

推荐做法：

```text
set {Ch1.PrivateChat.Target} = "zhou"
[signal arg="flow:enter_private_chat"]
```

变量保存上下文，`flow:*` 信号触发流程。

### 6.4 外部访问 Dialogic 变量

`FlowManager` 可以在收到流程信号后读取 Dialogic 变量：

```gdscript
private_chat_target = String(Dialogic.VAR.Ch1.PrivateChat.Target)
```

也可以用字符串路径访问：

```gdscript
var target: String = String(Dialogic.VAR.get("Ch1").get("PrivateChat").get("Target"))
Dialogic.VAR.set("Ch1.PrivateChat.Target", "zhou")
```

实际开发中建议封装一层 helper，避免路径散落：

```gdscript
func get_dialogic_var(path: String, default_value: Variant = null) -> Variant:
    var value: Variant = Dialogic.VAR.get(path)
    if value == null:
        return default_value
    return value
```

如果字符串路径在当前 Dialogic 版本下不支持点路径，就改成分段读取，不要在业务流程里到处手写分段。

### 6.5 对话结束后再执行场景动作

涉及切场景的流程信号，不建议在收到 signal 的同一帧立刻执行。更稳的处理方式：

```text
Dialogic timeline
-> 发出 flow:start_initial_search
-> FlowManager 记录 pending action
-> Dialogic.timeline_ended
-> FlowManager 执行切场景
-> SceneManager 完成加载
-> FlowManager 播放 pending_auto_timeline
```

这样可以避免 Dialogic UI 尚未关闭、暂停状态尚未恢复、过场动画同时抢状态的问题。

---

## 7. DialogueManager 补充

当前 `DialogueManager` 已经负责启动和结束 Dialogic。后续需要补两件事。

### 7.1 监听 Dialogic signal

Dialogic timeline 中使用统一格式：

```text
[signal arg="flow:start_initial_search"]
[signal arg="flow:enter_private_chat"]
[signal arg="flow:exit_private_chat"]
```

`DialogueManager` 收到后：

```gdscript
func _on_dialogic_signal(argument: String) -> void:
    if not argument.begins_with("flow:"):
        return
    EventBus.flow_signal_requested.emit(argument.trim_prefix("flow:"))
```

### 7.2 对话结束通知

Dialogic timeline 结束后：

```gdscript
EventBus.dialogue_finished.emit(_current_timeline_name)
```

`FlowManager` 可以在这里执行延迟动作，例如“对话结束后切场景”。

---

## 8. SceneManager 补充

`SceneManager` 在 `_load_room()` 完成后，通知当前房间加载完毕。

房间脚本需要增加：

```gdscript
@export var room_id: String = ""
```

房间场景需要统一增加：

```text
Room
├── NPCSpawnPoints
│   ├── Butler
│   ├── Zhou
│   ├── Mu
│   ├── Lin
│   ├── Wu
│   └── Zhong
└── DynamicActors
```

约定：

- `NPCSpawnPoints` 只放 `Marker2D`，表示 NPC 出现位置。
- `DynamicActors` 由 `FlowManager` 清空并挂载 NPC 实例。
- 剧情 NPC 不再长期手动摆在房间场景里。
- 固定装饰角色如果没有主线交互，可以留在场景里，但不要使用主线 `NpcDialogue`。

`SceneManager` 加载房间后：

```gdscript
var room_id: String = ""
if level_instance.get("room_id") != null:
    room_id = String(level_instance.room_id)
EventBus.room_loaded.emit(level_instance, room_id)
EventBus.room_presented.emit(level_instance, room_id)
```

其中 `room_loaded` 建议在黑幕期间发出，`room_presented` 在淡入完成后发出。如果某些旧房间还没配置 `room_id`，可以先用场景路径或节点名兜底。

---

## 9. 第一章流程节点

第一章建议先按以下节点推进：

| step_id | 内容 | 主要场景 | 说明 |
|---|---|---|---|
| `ch1_1_intro_hall` | 1.1 入局 | 大厅 | 玩家自由调查五名嫌疑人和管家 |
| `ch1_2_first_search` | 1.2 最初搜证 | 二楼/案发房间 | 场景切换后自动播放管家说明 |
| `ch1_3_initial_reasoning` | 1.3 最初推理 | 大厅 | 回大厅后自动播放多人讨论 |
| `ch1_4_private_chat` | 1.4 私聊 | 大厅/会客厅 | 大厅自由交互，选择私聊后切会客厅自动播放 |
| `ch1_5_second_search` | 1.5 第二轮搜证 | 储物间/研究所 | 流程自动对话和调查出口对话 |

---

## 10. 第一章信号设计

### 10.1 1.1 到 1.2

管家 `1_1_butler.dtl` 中，“准备好了”选项触发：

```text
[signal arg="flow:start_initial_search"]
```

`FlowManager` 处理：

```text
pending action = start_initial_search
```

`Dialogic.timeline_ended` 后：

```text
set_step("ch1_2_first_search")
set_npc_location("butler", "floor2", "Butler")
request_scene_change(二楼路径, 二楼出生点, "1_2_butler_a")
```

场景加载完成后，`FlowManager` 先根据全局 NPC 位置表把管家实例化到二楼 `NPCSpawnPoints/Butler`，再自动播放 `1_2_butler_a`。

`1_2_butler_a.dtl` 结束前触发：

```text
[signal arg="flow:enter_room_lin"]
```

`Dialogic.timeline_ended` 后：

```text
set_step("ch1_2_first_search")
set_npc_location("butler", "room_wu_ting_xiang", "Butler")
request_scene_change(乌停湘房间路径, 从二楼进入的出生点, "1_2_butler_b")
```

这样管家会从二楼迁移到乌停湘房间。之后玩家回大厅时，因为管家的全局位置仍然是 `room_wu_ting_xiang`，不会在大厅重复生成管家。

### 10.2 1.2 到 1.3

玩家完成搜证后，相关线索或对话触发：

```text
[signal arg="flow:finish_initial_search"]
```

管家确认返回大厅时触发：

```text
[signal arg="flow:start_initial_reasoning"]
```

`FlowManager` 处理：

```text
pending action = start_initial_reasoning
```

`Dialogic.timeline_ended` 后：

```text
set_step("ch1_3_initial_reasoning")
request_scene_change(大厅路径, 大厅出生点, "1_3_butler_lin_mu_wu_zhong_zhou")
```

### 10.3 1.3 到 1.4

`1_3_butler_lin_mu_wu_zhong_zhou.dtl` 结束前触发：

```text
[signal arg="flow:enable_private_chat"]
```

`FlowManager` 处理：

```text
set_step("ch1_4_private_chat")
```

同时保留或设置 Dialogic 变量：

```text
set {Ch1.PrivateChat.Enabled} = true
```

### 10.4 进入私聊

大厅中各 NPC 的自由交互 timeline 内，“邀请私聊”选项继续放在 `1_1_xxx.dtl` 中。选择后设置：

```text
set {Ch1.PrivateChat.Target} = "zhou"
[signal arg="flow:enter_private_chat"]
```

`FlowManager` 处理：

```text
private_chat_target = Dialogic.VAR.Ch1.PrivateChat.Target
pending action = enter_private_chat
```

`Dialogic.timeline_ended` 后：

```text
set_npc_location(private_chat_target, "hui_ke_ting", "Guest", "1_4_" + private_chat_target)
request_scene_change(会客厅路径, 私聊出生点, "1_4_" + private_chat_target)
```

场景加载完成后自动播放对应私聊 timeline。

### 10.5 退出私聊

私聊 timeline 结束或玩家选择离开时触发：

```text
[signal arg="flow:exit_private_chat"]
```

`FlowManager` 处理：

```text
pending action = exit_private_chat
```

`Dialogic.timeline_ended` 后：

```text
set_step("ch1_4_private_chat")
request_scene_change(大厅路径, 大厅返回点)
```

此时不需要自动播放对话。`set_step("ch1_4_private_chat")` 会重置该阶段的基础 NPC 位置，刚才被移动到会客厅的私聊对象会回到大厅，玩家继续自由行动。

### 10.6 进入第二轮搜证

管家私聊或大厅管家入口中，“下一轮调查”触发：

```text
[signal arg="flow:start_second_search"]
```

`FlowManager` 处理：

```text
pending action = start_second_search
```

`Dialogic.timeline_ended` 后：

```text
set_step("ch1_5_second_search")
request_scene_change(储物间或暗门入口路径, 出生点, "1_5_all")
```

---

## 11. 房间与 NPC 实例化

第一轮就迁移到动态实例化。原因是第一章从 1.1 到 1.5 已经涉及多场景、多角色站位、私聊场景切换，如果继续手动摆 NPC，很快会出现同一角色在不同流程中重复配置的问题。

当前采用“全局 NPC 位置表”方案：`FlowManager` 只保存每个 `npc_id` 当前位于哪个 `room_id`、使用哪个 `NPCSpawnPoints`。房间加载时只实例化位置属于当前房间的 NPC。这样同一角色在逻辑上全局唯一，不会因为多个房间配置里都写了管家而重复出现。

为避免切场时淡入结束后 NPC 才突然跳出来，房间进入现在分成两个阶段：

- `room_loaded`：新房间已经实例化，但还处于黑幕下。用于提前生成 NPC、线索等静态内容。
- `room_presented`：淡入完成，画面已经呈现给玩家。用于自动对话、进房演出等必须等画面稳定后再触发的逻辑。

### 11.1 基本规则

- 房间场景不手动摆主线 NPC。
- 房间只提供 `NPCSpawnPoints`。
- `FlowManager` 在 `room_loaded` 后根据 `_npc_locations` 实例化 NPC。
- 自动对话在 `room_presented` 后再触发，避免转场期间请求被丢掉。
- `_npc_locations` 中一个 `npc_id` 同一时间只允许有一条位置记录。
- 流程推进时通过 `set_step()` 重置基础站位，通过 `set_npc_location()` 移动单个 NPC。
- 实例化后为 NPC 设置 `npc_id`、`npc_name`、`timeline_name`。
- `timeline_name` 仍然保留，NPC 交互流程不需要改成完整路由。

### 11.2 基础位置表

第一版不必立刻创建 `NpcSpawnData` Resource。先在 `FlowManager` 用常量 Dictionary 写清楚每个流程节点的基础站位。

示例：

```gdscript
const NPC_SCENE_PATH: String = "res://scenes/characters/npcs/npc.tscn"

const BASE_NPC_LOCATIONS_BY_STEP: Dictionary = {
    "ch1_1_intro_hall": {
        "butler": {"room_id": "hall", "spawn": "Butler"},
        "zhou": {"room_id": "hall", "spawn": "Zhou"},
        "mu": {"room_id": "hall", "spawn": "Mu"},
        "lin": {"room_id": "hall", "spawn": "Lin"},
        "wu": {"room_id": "hall", "spawn": "Wu"},
        "zhong": {"room_id": "hall", "spawn": "Zhong"},
    },
    "ch1_2_first_search": {
        "butler": {"room_id": "floor2", "spawn": "Butler"},
        "zhou": {"room_id": "hall", "spawn": "Zhou"},
        "mu": {"room_id": "hall", "spawn": "Mu"},
        "lin": {"room_id": "hall", "spawn": "Lin"},
        "wu": {"room_id": "hall", "spawn": "Wu"},
        "zhong": {"room_id": "hall", "spawn": "Zhong"},
    },
    "ch1_4_private_chat": {
        "butler": {"room_id": "hall", "spawn": "Butler"},
        "zhou": {"room_id": "hall", "spawn": "Zhou"},
        "mu": {"room_id": "hall", "spawn": "Mu"},
        "lin": {"room_id": "hall", "spawn": "Lin"},
        "wu": {"room_id": "hall", "spawn": "Wu"},
        "zhong": {"room_id": "hall", "spawn": "Zhong"},
    },
}
```

`set_step(step_id)` 执行时，把 `BASE_NPC_LOCATIONS_BY_STEP[step_id]` 深拷贝到 `_npc_locations`。之后流程中的临时移动只改 `_npc_locations`，不改基础表。

### 11.3 单个 NPC 移动

单个 NPC 的跨场景移动统一通过 `set_npc_location()`：

```gdscript
func set_npc_location(
    npc_id: String,
    room_id: String,
    spawn_name: String,
    timeline_name: String = "",
    scene_path: String = ""
) -> void:
    if npc_id.is_empty():
        return
    if room_id.is_empty() or spawn_name.is_empty():
        _npc_locations.erase(npc_id)
        return

    var location: Dictionary = {
        "room_id": room_id,
        "spawn": spawn_name,
    }
    if not timeline_name.is_empty():
        location["timeline"] = timeline_name
    if not scene_path.is_empty():
        location["scene"] = scene_path
    _npc_locations[npc_id] = location
```

典型用法：

```gdscript
set_npc_location("butler", "floor2", "Butler")
set_npc_location("butler", "room_wu_ting_xiang", "Butler")
set_npc_location(private_chat_target, "hui_ke_ting", "Guest", "1_4_%s" % private_chat_target)
```

这比按 `step_id + room_id` 重复声明 NPC 更安全，因为同一 NPC 不会同时出现在大厅、二楼和案发房间。

### 11.4 动态实例化流程

```text
SceneManager 加载房间
-> EventBus.room_loaded(room, room_id)
-> FlowManager.on_room_loaded(room, room_id)
-> FlowManager 清空 DynamicActors
-> FlowManager 从 _npc_locations 筛出 room_id == current_room_id 的 NPC
-> 实例化这些 NPC
-> 淡入完成
-> EventBus.room_presented(room, room_id)
-> FlowManager.on_room_presented(room, room_id)
-> FlowManager.play_pending_auto_timeline()
-> 放到 NPCSpawnPoints/{spawn}
-> apply_free_timeline(npc)
-> 如果 spawn_data 带 timeline override，则覆盖自由交互 timeline
-> 如有 pending_auto_timeline，播放自动对话
```

### 11.5 FlowManager 伪代码

```gdscript
func _reset_npc_locations_for_step(step_id: String) -> void:
    _npc_locations.clear()

    var base_locations: Dictionary = BASE_NPC_LOCATIONS_BY_STEP.get(step_id, {})
    for npc_id in base_locations.keys():
        _npc_locations[String(npc_id)] = base_locations[npc_id].duplicate(true)


func _get_spawn_entries_for_room(room_id: String) -> Array:
    var spawn_entries: Array = []
    for npc_id in _npc_locations.keys():
        var location: Dictionary = _npc_locations[npc_id]
        if String(location.get("room_id", "")) != room_id:
            continue

        var spawn_data: Dictionary = location.duplicate(true)
        spawn_data["npc_id"] = String(npc_id)
        spawn_entries.append(spawn_data)
    return spawn_entries


func setup_room_actors(room: Node2D) -> void:
    var dynamic_root: Node2D = room.find_child("DynamicActors", true, false) as Node2D
    if dynamic_root == null:
        return

    for child: Node in dynamic_root.get_children():
        child.queue_free()

    var spawns: Array = _get_spawn_entries_for_room(current_room_id)
    for spawn_data: Dictionary in spawns:
        spawn_npc(spawn_data, room)


func spawn_npc(spawn_data: Dictionary, room: Node2D) -> void:
    var npc_id: String = String(spawn_data.get("npc_id", ""))
    if npc_id.is_empty():
        return

    var spawn_name: String = String(spawn_data.get("spawn", ""))
    var spawn_point: Marker2D = room.find_child(spawn_name, true, false) as Marker2D
    var dynamic_root: Node2D = room.find_child("DynamicActors", true, false) as Node2D
    if spawn_point == null or dynamic_root == null:
        return

    var scene: PackedScene = load(NPC_SCENE_PATH)
    var npc: NpcDialogue = scene.instantiate() as NpcDialogue
    if npc == null:
        return

    npc.npc_id = npc_id
    apply_free_timeline(npc)
    if spawn_data.has("timeline"):
        npc.timeline_name = String(spawn_data["timeline"])
    dynamic_root.add_child(npc)
    npc.global_position = spawn_point.global_position
```

### 11.6 后续再资源化

当 Dictionary 变得太长，再迁移到 Resource：

- `NpcSpawnData`
- `RoomActorSetupData`
- `FlowStepData`

第一轮先用代码常量，方便快速落地和调试。

---

## 12. 状态存放原则

### 12.1 放在 Dialogic 变量

以下状态可以继续放在 Dialogic：

- 对话选项是否点过。
- NPC 是否介绍过。
- 搜身一次/二次状态。
- 私聊话题是否问过。
- 私聊选项是否开启。
- 某些一次性话题是否隐藏。

原因：这些状态主要影响 timeline 内部的文本和选项。

### 12.2 放在 FlowManager 或 DataManager

以下状态建议放在流程层：

- 当前章节。
- 当前流程节点。
- 当前房间。
- 当前私聊对象。
- 当前 NPC 全局位置表。
- 待播放自动 timeline。
- 对话结束后待执行动作。

原因：这些状态会影响场景切换、自动对话、NPC 实例化和存档恢复。

### 12.3 短期同步策略

短期可以允许 Dialogic 变量和 `FlowManager` 同时存在，但要明确主次：

- timeline 内部显示逻辑，以 Dialogic 变量为准。
- 场景转换和自动对话，以 `FlowManager` 为准。
- NPC 当前在哪个房间、哪个挂点，以 `FlowManager._npc_locations` 为准。
- 存档系统后续接入时，需要把关键流程状态落到 `DataManager`。

---

## 13. 实施顺序

第一轮建议只实现轻量流程闭环：

1. 新增 `FlowManager` Autoload。
2. 新增 `EventBus.flow_signal_requested`、`EventBus.room_loaded`、`EventBus.room_presented`、`EventBus.dialogue_finished`。
3. `DialogueManager` 连接 `Dialogic.signal_event`，并转发 `flow:*`。
4. `DialogueManager` 在 timeline 结束时发出 `dialogue_finished`。
5. `SceneManager` 在黑幕下房间加载完成后发出 `room_loaded`，淡入完成后发出 `room_presented`。
6. 给主线房间增加 `NPCSpawnPoints` 和 `DynamicActors`。
7. `NpcDialogue` 增加 `npc_id`，保留 `timeline_name`。
8. `FlowManager` 实现 `_npc_locations` 全局 NPC 位置表，并通过 `set_step()` 重置基础站位。
9. `FlowManager` 实现 `set_npc_location()`，用于流程中移动单个 NPC。
10. `FlowManager` 在 `room_loaded` 后只实例化当前位置属于当前房间的 NPC，并根据 `current_chapter_id/current_step_id` 赋值 `timeline_name`。
11. `FlowManager` 实现 `pending_auto_timeline`，并在 `room_presented` 后自动请求对话。
12. `FlowManager` 实现 `_pending_action_after_dialogue`，涉及切场景的 flow 信号先记录，等 `dialogue_finished` 再执行。
13. 在 `1_1_butler.dtl` 中接入 `flow:start_initial_search`。
14. 在 `1_2_butler_a.dtl` 中接入 `flow:enter_room_lin`，把管家从二楼移动到乌停湘房间。
15. 验证“动态刷大厅 NPC -> 管家对话 -> 发 flow signal -> 对话结束 -> 切到二楼 -> 自动播放 `1_2_butler_a` -> 切到乌停湘房间 -> 自动播放 `1_2_butler_b`”。

第一轮不做：

- 不改造 `NpcDialogue.timeline_name`。
- 不引入 `DialogueRouteData`。
- 不重构已有 `1_1_xxx.dtl`。

---

## 14. 第一轮验收标准

最小可验证目标：

```text
玩家进入大厅
-> FlowManager 动态实例化大厅 NPC
-> FlowManager 为 NPC 设置 npc_id 和 timeline_name
-> 点击 NPC 仍然走原有 1_1_xxx 自由交互 timeline
-> 与管家对话选择进入搜证
-> Dialogic 发出 flow:start_initial_search
-> FlowManager 记录 pending action
-> Dialogic.timeline_ended
-> FlowManager 推进到 ch1_2_first_search
-> FlowManager 将 butler 位置设为 floor2/Butler
-> SceneManager 切到二楼
-> room_loaded 后先在黑幕下实例化二楼管家
-> room_presented 后自动播放 1_2_butler_a
-> 1_2_butler_a 发出 flow:enter_room_lin
-> Dialogic.timeline_ended
-> FlowManager 将 butler 位置设为 room_wu_ting_xiang/Butler
-> SceneManager 切到乌停湘房间
-> room_loaded 后先在黑幕下实例化案发房间管家
-> room_presented 后自动播放 1_2_butler_b
-> 玩家回大厅时不会重复生成管家
-> 对话结束后玩家恢复自由行动
```

这个闭环跑通后，再继续接：

- `flow:start_initial_reasoning`
- `flow:enable_private_chat`
- `flow:enter_private_chat`
- `flow:exit_private_chat`
- `flow:start_second_search`

---

## 15. 后续扩展时机

只有在出现以下问题时，再考虑完整数据化路由：

- 同一个场景里同一个 NPC 的自由交互 timeline 变得过长，维护困难。
- 大量 NPC 的 `timeline_name` 需要根据流程动态切换。
- `BASE_NPC_LOCATIONS_BY_STEP` 和流程移动逻辑变得过长，不适合继续写在 `FlowManager` 中。
- 后续章节出现大量“同房间不同时间段完全不同 NPC 布置”的情况，需要编辑器资源辅助维护。

到那时再引入：

- `DialogueRouteData`
- `FlowStepData`
- `RoomSetupData`
- `NpcSpawnData`

当前阶段先把流程自动对话跑通，成本最低，收益最高。
