# 《异数 (Abismo)》程序说明文档

## 1. 文件结构

```text
Abismo/
├── addons/                    # 第三方插件（Dialogic 2 等）
├── assets/                    # 美术与音频资源
│   ├── characters/            # 角色资源（含 npcs/*.dch 对话角色文件）
│   ├── dialogues/             # Dialogic 对话时间线 (*.dtl)
│   ├── game/                  # 通用基础资源
│   ├── interior/              # 室内场景美术
│   ├── objects/               # 物件与道具资源
│   └── UI/                    # 界面贴图与字体
├── scenes/                    # 场景文件 (.tscn)
│   ├── characters/            # 角色预制体
│   ├── objects/               # 物件预制体
│   ├── rooms/                 # 游戏房间场景
│   ├── test/                  # 测试用场景
│   └── UI/                    # UI 组件预制体
├── scripts/                   # 脚本 (.gd)
│   ├── characters/            # 角色控制与状态机
│   ├── objects/               # 物件与交互逻辑
│   ├── scenes/                # 场景管理
│   └── UI/                    # 界面逻辑
├── Tilesets/                  # 瓦片集配置 (.tres)
└── project.godot              # 项目配置
```

## 2. 程序说明

### 2.1 全局单例

#### 2.1.1 事件总线 (EventBus)

脚本路径：`scripts/event_bus.gd`

作用：

作为无状态行为请求总线，负责跨模块派发“去做某事”的事件（如请求切场景、请求发起对话），避免业务层之间相互硬引用。

包含内容：

- `signal scene_change_requested(target_scene_path: String, spawn_point_name: String)`：架构标准场景切换请求信号，供后续 SceneManager 流程使用。
- `signal dialogue_requested(request: Variant)`：对话发起请求信号。
  - 兼容旧格式：`String`（直接作为 timeline_name）。
  - 预留新格式：`Dictionary`（可传 `timeline_name` 或 `npc_id + entry_id`）。
- `signal clue_interaction_details_requested(payload: Dictionary)`：线索交互详情请求信号（由 `ClueItem` 发出，供后续 `ClueUI` 的 `interaction` 模式消费）。
- `signal clue_selection_requested(context: Dictionary)`：推理界面请求线索手册进入选择模式。
- `signal clue_selected_for_reasoning(context: Dictionary, clue_id: String)`：线索手册完成选择后回传给推理界面。
- `signal ui_panel_focus_requested(panel_id: String)`：用于协调手册类 UI 的互斥显示，同一时间只保留一个面板。

#### 2.1.2 数据管理器 (DataManager)

脚本路径：`scripts/data_manager.gd`

作用：

作为全局运行时数据中心，统一维护线索、疑点、世界状态与好感度等核心字典，并在状态首次变化时发出数据通知与提示信号。

包含内容：

- `signal clue_updated(clue_id: String)`：线索状态更新通知。
- `signal suspicion_updated(suspicion_id: String)`：疑点状态更新通知。
- `signal affinity_changed(npc_id: String, new_value: int)`：好感度变化通知（骨架信号）。
- `signal ui_notice_requested(message: String, notice_type: String)`：通用提示请求信号（用于 Toast UI）。
- `var clue_defs: Dictionary[String, ClueData]`：线索静态定义缓存。
- `var suspicion_defs: Dictionary[String, SuspicionData]`：疑点静态定义缓存。
- `var clue_states: Dictionary[String, Dictionary]`：线索运行时状态缓存（`discovered`、`discover_order`、`read`、`first_source_type`、`first_source_id`、`deep_unlocked`）。
- `var suspicions: Dictionary[String, Dictionary]`：疑点运行时状态缓存（`discovered`、`discover_order`、`read`、`resolved`、`first_source_type`、`first_source_id`、`conclusion_clue_id`）。
- `var world_flags: Dictionary[String, bool]`：世界交互状态字典，用于记录交互是否已触发。
- `var affinity: Dictionary[String, int]`：NPC 好感度运行时存储字典。
- `func _ready() -> void`：Autoload 初始化时递归扫描 `res://assets/objects/clues/` 与 `res://assets/objects/suspicions/` 下全部 `.tres` 资源，并自动注册为 `clue_defs` / `suspicion_defs`。
- `func register_clue_def(clue_def: ClueData) -> void`：注册单条线索定义。
- `func register_clue_defs(clue_def_list: Array[ClueData]) -> void`：批量注册线索定义。
- `func register_suspicion_def(suspicion_def: SuspicionData) -> void`：注册单条疑点定义。
- `func register_suspicion_defs(suspicion_def_list: Array[SuspicionData]) -> void`：批量注册疑点定义。
- `func add_clue(clue_id: String, source_type: String = "scene", source_id: String = "") -> bool`：线索发现写入；首次返回 `true`，重复调查返回 `false`；当 `clue_id` 尚未注册静态定义时输出警告，便于定位资源漏挂载或漏注册问题。
- `func has_clue(clue_id: String) -> bool`：判定线索是否已发现。
- `func mark_clue_read(clue_id: String) -> void`：标记线索已读。
- `func get_clue_state(clue_id: String) -> Dictionary`：读取单条线索状态快照。
- `func get_discovered_clues() -> PackedStringArray`：按 `discover_order` 返回已发现线索 ID 列表。
- `func get_clues_by_category_path(path: PackedStringArray) -> PackedStringArray`：按分类路径筛选已发现线索。
- `func get_clues_by_tag(tag: String) -> PackedStringArray`：按标签筛选已发现线索。
- `func get_parent_clue_id(clue_id: String) -> String`：读取父线索 ID。
- `func get_child_clue_ids(clue_id: String) -> PackedStringArray`：读取子线索 ID 列表。
- `func is_deep_unlocked(parent_clue_id: String) -> bool`：读取父线索是否已完成深入调查。
- `func mark_deep_unlocked(parent_clue_id: String) -> bool`：首次深入调查写入并返回 `true`，并在该时机补登记父线索下的子线索到已发现列表（写入 `discover_order`）；后续重复返回 `false`。
- `func add_suspicion(suspicion_id: String, source_type: String = "dialogue", source_id: String = "") -> bool`：疑点发现写入；首次返回 `true`，重复触发返回 `false`。
- `func has_suspicion(suspicion_id: String) -> bool`：判定疑点是否已发现。
- `func get_suspicion_state(suspicion_id: String) -> Dictionary`：读取单条疑点状态快照。
- `func get_all_suspicions() -> PackedStringArray`：按 `discover_order` 返回已发现疑点 ID 列表。
- `func is_suspicion_resolved(suspicion_id: String) -> bool`：判定疑点是否已解决。
- `func mark_suspicion_read(suspicion_id: String) -> void`：标记疑点已读，用于红点消除。
- `func resolve_suspicion(suspicion_id: String) -> bool`：标记疑点已解决，并通过 `add_clue(conclusion_clue_id, "reasoning", suspicion_id)` 写入结论线索。
- `func set_world_flag(flag_id: String, value: bool = true) -> void`：设置世界状态标记；当 `flag_id` 为空时直接返回，避免写入无效键。
- `func get_world_flag(flag_id: String) -> bool`：读取世界状态标记；当 `flag_id` 为空时返回 `false`，否则返回字典中的标记值。

#### 2.1.3 场景管理器 (SceneManager)

脚本路径：`scripts/scenes/scene_manager.gd`

作用：

作为场景切换与玩家常驻的单例中心，接管 `game_root.gd` 中房间切换、过场淡入淡出、玩家跨房间复用与出生点落位逻辑。

包含内容：

- `func initialize(host_root: Node2D, first_level_path: String, first_spawn_point: String) -> void`：初始化挂载容器、创建常驻玩家、加载首个房间。
- `func _on_scene_change_requested(target_path: String, spawn_point_name: String) -> void`：处理 `scene_change_requested`，执行防抖、暂停、过场动画、旧房间清理与新房间加载。
- `func _ensure_level_container() -> void`：确保房间容器存在并挂载到当前 `GameRoot`。
- `func _ensure_player() -> void`：确保全局唯一玩家实例存在。
- `func _load_room(path: String, spawn_point_name: String) -> void`：实例化房间、挂载常驻玩家、按出生点放置并配置相机边界。
- `var is_transitioning: bool`：过场中的重入保护标记，供其他系统查询。

#### 2.1.4 游戏管理器 (GameManager)

脚本路径：`scripts/game_manager.gd`

作用：

统一管理全局运行状态（主菜单/游戏中/暂停/对话中）与暂停入口，避免暂停逻辑分散在多个业务脚本中导致状态冲突。

包含内容：

- `enum GameState`：定义 `MAIN_MENU`、`IN_GAME`、`PAUSED`、`DIALOGUE`。
- `func enter_main_menu() -> void`：切换到主菜单状态，清空暂停令牌并恢复树运行。
- `func enter_gameplay() -> void`：切换到游戏中状态。
- `func request_pause(token: String) -> void` / `func release_pause(token: String) -> void`：基于令牌的暂停引用计数式管理（例如用户暂停、对话暂停、过场暂停）。
- `func toggle_user_pause() -> void`：统一处理用户 Esc 暂停开关。
- `func start_dialogue_state() -> void` / `func end_dialogue_state() -> void`：统一对话期间的状态切换与暂停控制。
- `signal game_state_changed(previous_state: int, new_state: int)`：游戏状态变更通知。
- `signal game_pause_changed(is_paused: bool)`：暂停状态变更通知。

#### 2.1.5 对话管理器 (DialogueManager)

脚本路径：`scripts/dialogue/dialogue_manager.gd`

作用：

统一监听对话请求并接管 Dialogic 启停、防重入、结束回调与暂停恢复，作为 NPC 与 Dialogic 之间的唯一业务入口。

包含内容：

- `func _on_dialogue_requested(request: Variant) -> void`：接收 EventBus 对话请求，执行防重入校验并解析目标时间线。
- `func _resolve_timeline_name(request: Variant) -> String`：兼容旧版 `String` 请求，同时支持 `Dictionary` 请求（`timeline_name` 或 `npc_id + entry_id`）。
- `func _resolve_timeline_by_route(npc_id: String, entry_id: String) -> String`：Task 2.3 预留路由入口，后续可替换为资源表匹配。
- `func _start_dialogue(timeline_name: String) -> void`：统一启动 Dialogic，并接入 GameManager 的对话状态管理。
- `func _on_dialogue_ended() -> void`：统一处理对话结束与状态恢复；场景过渡期间避免提前恢复。

### 2.2 交互组件

#### 2.2.1 可交互对象基类 (Interactable)

脚本路径：`scripts/objects/interactable/interactable.gd`

作用：

定义所有可交互对象的统一基类（`Area2D`），约束交互对象的公共属性与响应入口，并在节点初始化时根据 `DataManager.world_flags` 自动处理一次性对象的场景重入自销毁。

包含内容：

- `@export var interact_id: String`：交互对象唯一标识，用于 world_flags 查询与持久化标记。
- `@export var is_one_time: bool`：标记该交互对象是否为一次性交互（供子类逻辑使用）。
- `@export var prompt_offset: Vector2`：交互提示相对偏移量（每个对象可单独在检查器中精调提示位置）。
- `func _ready() -> void`：仅当 `is_one_time = true` 时才检查 `interact_id`；若在 `DataManager` 中已标记触发，则执行 `queue_free()` 自毁，防止一次性对象重复出现。
- `func interact(player: Player) -> void`：交互虚方法；默认仅给出警告，要求子类覆盖实现具体行为。

#### 2.2.2 玩家交互区域组件

脚本路径：`scripts/characters/player/player_interaction_area.gd`

挂载位置：`player.tscn` 场景中，`Player` 下的 `InteractionArea` 节点

作用：

挂载在 Player 下的交互检测组件，负责范围扫描、最近目标仲裁、交互输入处理与提示显隐，实现“Player 负责检测与仲裁、Interactable 负责响应”的架构分工。

包含内容：

- `@export var player: Player`：玩家引用，用于距离计算与调用交互接口。
- `@export var prompt_sprite_frames: SpriteFrames`：交互提示动画资源（当前使用 `assets/UI/key_F_frames.tres`）。
- `@export var prompt_vertical_padding: float`：提示与交互对象顶部的垂直间距。
- `var _interactables: Array[Interactable]`：当前进入交互范围的可交互对象列表。
- `var _current_target: Interactable`：当前仲裁出的最近交互目标。
- `var _prompt_ui: AnimatedSprite2D`：运行时创建的提示动画节点（世界坐标显示）。
- `func _ready() -> void`：连接 `area_entered/area_exited` 信号，初始化隐藏提示，并在未显式赋值时自动绑定父级 `Player`。
- `func _physics_process(_delta: float) -> void`：每个物理帧刷新最近目标；若玩家引用为空则隐藏提示并退出。
- `func _unhandled_input(event: InputEvent) -> void`：检测 F 键交互输入；当存在当前目标时调用目标的 `interact(player)`。
- `func _on_area_entered(area: Area2D) -> void`：当 `Interactable` 进入范围时加入列表（去重）。
- `func _on_area_exited(area: Area2D) -> void`：当 `Interactable` 离开范围时从列表移除。
- `func _refresh_target() -> void`：清理失效引用并按与玩家距离计算最近目标，同时更新提示显隐。
- `func _prune_invalid_interactables() -> void`：移除已失效（被释放）的交互对象引用。
- `func _set_interaction_hint_visible(is_visible: bool) -> void`：统一控制提示动画节点显示与播放状态。
- `func _ensure_prompt_ui() -> void`：创建并初始化运行时提示动画节点。
- `func _update_prompt_transform() -> void`：将提示固定到当前交互目标上方，而非跟随玩家。
- `func _get_prompt_global_position(interactable: Interactable) -> Vector2`：计算提示应显示的世界坐标，并叠加目标对象自身 `prompt_offset` 以实现逐对象精调。
- `func _find_primary_collision_shape(interactable: Interactable) -> CollisionShape2D`：获取目标主要碰撞体以辅助提示定位。
- `func _is_interact_input(event: InputEvent) -> bool`：判定是否为有效的 F 键按下事件（过滤长按回响与非键盘输入）。

#### 2.2.3 NPC 对话交互组件 (NpcDialogue)

脚本路径：`scripts/characters/npc/npc.gd`

挂载位置：`scenes/characters/npcs/npc.tscn` 场景根节点

作用：

将 NPC 重构为继承 `Interactable` 的被动交互对象，不再自行检测输入与重叠；当玩家交互组件调用 `interact()` 时，通过 `EventBus` 派发对话请求。

包含内容：

- `@export var npc_name: String`：NPC 名称（用于编辑器配置与日志提示）。
- `@export var timeline_name: String`：Dialogic 时间线标识。
- `func interact(_player: Player) -> void`：NPC 交互入口；当 `timeline_name` 有效时发出 `EventBus.dialogue_requested.emit(timeline_name)`，否则给出警告并返回。

#### 2.2.4 门交互组件 (Door)

脚本路径：`scripts/scenes/door.gd`

作用：

门节点的交互触发实现。该脚本已改为继承 `Interactable`，不再通过 `body_entered` 自动切场景，而是由玩家交互组件调用 `interact()` 后再发起切换请求。

包含内容：

- `@export_file("*.tscn") var target_scene_path: String`：目标场景路径。
- `@export var target_spawn_point: String`：目标场景出生点名称。
- `func interact(_player: Player) -> void`：门交互入口；当目标场景有效时发出 `EventBus.scene_change_requested.emit(target_scene_path, target_spawn_point)`，否则报错并返回。

#### 2.2.5 过道切换组件 (Passage)

脚本路径：`scripts/scenes/passage.gd`

作用：

过道节点的自动触发切换实现。用于“无需按交互键、进入触发区即切换”的通道场景连接。

包含内容：

- `@export_file("*.tscn") var target_scene_path: String`：目标场景路径。
- `@export var target_spawn_point: String`：目标场景出生点名称。
- `func _on_body_entered(body: Node2D) -> void`：仅当进入者属于 `player` 组时触发；发出 `EventBus.scene_change_requested` 请求。

#### 2.2.6 线索交互组件 (ClueItem)

脚本路径：`scripts/objects/interactable/clue_item.gd`

作用：

基于 `Interactable` 的线索交互实现，负责将场景调查行为写入 `DataManager`，并根据交互轮次发出“单线索详情”或“父+子分层详情”请求。

包含内容：

- `@export var clue_id: String`：线索 ID（为空时回退到 `clue_def.id` 或 `interact_id`）。
- `@export var clue_def: ClueData`：当前线索定义（`_ready()` 时自动注册到 `DataManager`）。
- `@export var child_clue_defs: Array[ClueData]`：可选子线索定义列表（用于深入调查配置并自动注册）。
- `@export var source_id: String`：调查来源 ID（为空时自动回退到 `interact_id` / `clue_id` / 节点名）。
- `func interact(_player: Player) -> void`：线索交互入口；规则为：
  - 一般线索：每次发出单线索详情请求。
  - 可深入调查线索：首次交互发出父线索单条详情；第二次及后续交互发出父+子分层详情。
  - 交互过程中调用 `DataManager.add_clue()`、`is_deep_unlocked()`、`mark_deep_unlocked()`。
- `func _emit_single_detail(...)` / `func _emit_hierarchical_detail(...)`：统一通过 `EventBus.clue_interaction_details_requested` 派发交互详情载荷。

### 2.3 推理系统

#### 2.3.1 线索数据结构

脚本路径：`scripts/objects/interactable/clue_data.gd`

作用：

定义线索静态配置资源 `ClueData`，用于在编辑器中以数据方式维护线索基础信息、UI 分类标签与深入调查父子关系。

包含内容：

- `@export var id: String`：线索唯一 ID。
- `@export var title: String`：线索标题。
- `@export_multiline var description: String`：线索描述文本。
- `@export var chapter_id: String`：所属章节 ID。
- `@export var room_id: String`：所属房间 ID。
- `@export var category_path: PackedStringArray`：线索栏分层路径。
- `@export var tags: PackedStringArray`：搜索与筛选标签。
- `@export_enum("scene_evidence", "testimony", "conclusion", "lore") var clue_type: String`：线索类型。
- `@export var is_conclusion: bool`：是否为结论线索。
- `@export var highlight_visible: bool`：是否显示高亮提示（仅视觉用途，不影响可调查性）。
- `@export var parent_clue_id: String`：父线索 ID（为空表示顶层线索）。
- `@export var child_clue_ids: PackedStringArray`：子线索 ID 列表（用于深入调查内容组织）。

当前用法：

- `scene_evidence`：场景调查、搜身等实物或观察类线索。
- `testimony`：口供线索。口供资源同样使用 `ClueData`，通过 Dialogic 时间线中的 `DataManager.add_clue()` 在角色说出对应内容后加入线索栏。
- 口供线索文件命名格式为 `{章节号}_testimony_{口供编号}_{角色}.tres`，例如 `1_testimony_2_butler.tres`；多人汇总口供可使用 `all` 作为角色段。
- 口供线索建议放在对应章节目录中，例如 `assets/objects/clues/ch1_snow_villa/`，并使用 `category_path = PackedStringArray("雪墅", "口供线索", "...")` 归档。
- 如果口供存在补充内容，可使用 `parent_clue_id` / `child_clue_ids` 建立父子关系；当前雪墅 1.3 中 `1_testimony_6_lin` 作为 `1_testimony_4_all` 的子线索。

#### 2.3.2 疑点数据结构

脚本路径：`scripts/logic/suspicion_data.gd`

作用：

定义疑点静态配置资源 `SuspicionData`，用于在编辑器中维护疑点文本、分类、搜索标签、所需线索与结论线索配置。

包含内容：

- `@export var id: String`：疑点唯一 ID。
- `@export var title: String`：疑点标题。
- `@export_multiline var description: String`：疑点正文描述。
- `@export var chapter_id: String`：所属章节 ID。
- `@export var category_path: PackedStringArray`：推理手册左侧树形目录路径。
- `@export var tags: PackedStringArray`：搜索匹配标签。
- `@export var required_clue_ids: PackedStringArray`：解决该疑点所需的线索 ID 集合。
- `@export_enum("exact", "contains") var match_mode: String`：推理匹配模式。
- `@export var conclusion_clue_id: String`：推理成功后生成的结论线索 ID。

#### 2.3.3 疑点与推理运行逻辑

脚本路径：`scripts/logic/reasoning_system.gd`

当前运行规则：

- 疑点由 `DataManager.add_suspicion()` 写入运行时状态，当前进度下实际触发来源为第一章 Dialogic 时间线中的自定义调用。
- 推理校验由 `ReasoningSystem.validate(suspicion_def, selected_clue_ids)` 统一负责。
- 当前推理 UI 固定为 3 个线索槽位；提交前必须全部填满。
- 同一个线索不能在同一个疑点的多个槽位中重复选择。
- `match_mode = "exact"` 与 `match_mode = "contains"` 当前都要求“所选集合与需求集合完全一致”；差异预留给后续扩展。
- 校验成功后调用 `DataManager.resolve_suspicion(suspicion_id)`：
  - 将疑点标记为 `resolved = true`；
  - 将疑点标记为已读；
  - 将 `conclusion_clue_id` 写回疑点状态；
  - 调用 `add_clue(conclusion_clue_id, "reasoning", suspicion_id)` 收录结论线索。
- 校验失败时当前实现不弹出额外提示，也不自动清空已选槽位。

#### 2.3.4 当前落地范围

- 线索可通过场景交互或 Dialogic 事件写入 `DataManager`。
- 疑点当前已接入运行时系统，但触发来源先只落地“第一章对话事件”。
- 场景交互触发疑点、结论再推导出新疑点等路径，保留为后续扩展点。

### 2.4 UI 系统

#### 2.4.1 线索栏组件 (ClueUI)

脚本路径：`scripts/UI/clue_ui.gd`

场景路径：`scenes/UI/clue_panel.tscn`

挂载位置：`scenes/game_root.tscn` 下的 `CluePanel` 子节点

作用：

实现线索手册展示组件，当前统一承载“线索手册浏览（archive）”“交互详情展示（interaction）”和“推理选线索（selection）”三种模式；支持目录分层折叠、关键词筛选、线索详情展示、推理选线索返回，以及打开面板后的背景轻度变暗。

包含内容：

- 固定面板逻辑尺寸 `1280x800`，在当前视口中居中并等比缩放。
- 全屏遮罩 `Dimmer`（黑色半透明）用于打开面板时让背景游戏画面稍稍变暗。
- `archive` 模式：
  - 读取 `DataManager.get_discovered_clues()`；
  - 按 `category_path` 构建目录树；
  - 深入调查子线索按父子层级挂到父线索下，不单独建立“深入调查”目录；
  - 支持按标题/描述/标签/分类文本搜索过滤；
  - 支持排序切换：默认按 `线索.txt` 固定顺序，可切换为按 `discover_order`；
  - 用户点击条目后展示详情并调用 `DataManager.mark_clue_read(clue_id)` 标记已读；刷新后保持当前选中，不回跳首条。
- `interaction` 模式：
  - 监听 `EventBus.clue_interaction_details_requested(payload)`；
  - 单线索交互展示单条；
  - 深入调查展示父线索及其子线索层级。
- `selection` 模式：
  - 由 `EventBus.clue_selection_requested(context)` 打开；
  - 左侧仍使用 archive 树形目录；
  - 右侧详情面板底部显示“选择线索”按钮；
  - 点击后通过 `EventBus.clue_selected_for_reasoning(context, clue_id)` 将所选线索回传给推理手册。
- 输入与状态：
  - `C` 键开关线索栏（进入/退出 `archive` 模式）；
  - 面板打开期间支持 `Esc` 关闭；
  - 若打开时收到 `EventBus.ui_panel_focus_requested("suspicion_panel")`，则主动关闭自己，实现与推理手册互斥切换；
  - 通过 `GameManager.request_pause("clue_panel") / release_pause("clue_panel")` 纳入统一暂停令牌管理。
- 数据刷新：监听 `DataManager.clue_updated`，在可见状态下刷新目录与详情。
- 稳定性约束：目录刷新采用 deferred 队列刷新，避免“已读写回触发刷新”导致的重入空引用。

#### 2.4.2 推理手册组件 (SuspicionUI)

脚本路径：`scripts/UI/suspicion_ui.gd`

场景路径：`scenes/UI/suspicion_panel.tscn`

挂载位置：`scenes/game_root.tscn` 下的 `SuspicionPanel` 子节点

作用：

实现疑点浏览与推理操作界面。整体布局与线索手册一致，标题为“推理手册”，当前仅提供 archive 型目录浏览，不单独实现 interaction 型疑点详情模式。

包含内容：

- 固定面板逻辑尺寸 `1280x800`，在当前视口中居中并等比缩放。
- 左侧为疑点树形目录：
  - 读取 `DataManager.get_all_suspicions()`；
  - 按 `SuspicionData.category_path` 构建树；
  - 支持按标题/描述/标签/分类文本搜索过滤；
  - 支持默认顺序 / 按发现时间排序切换；
  - 未读疑点显示红点；
  - 已解决疑点显示状态标签。
- 右侧上半区显示疑点标题与正文，并在点击时调用 `DataManager.mark_suspicion_read(suspicion_id)` 消除红点。
- 右侧下半区为推理面板：
  - 固定 3 个线索槽位与一个“推理”按钮；
  - 点击槽位后通过 `EventBus.clue_selection_requested(context)` 切换到线索手册 `selection` 模式；
  - 从线索手册返回后，将所选线索标题写入对应槽位；
  - 若选择了已在其他槽位中使用的线索，则旧槽位自动清空，保证同一线索不重复占用。
- 成功推理后，复用线索手册 `interaction` 模式展示结论线索；失败时当前实现不额外提示。
- 输入与状态：
  - `V` 键开关推理手册；
  - 面板打开期间支持 `Esc` 关闭；
  - 若打开时收到 `EventBus.ui_panel_focus_requested("clue_panel")`，则主动关闭自己，实现与线索手册互斥切换；
  - 通过 `GameManager.request_pause("suspicion_panel") / release_pause("suspicion_panel")` 纳入统一暂停令牌管理。

## 3. 变更记录

### 3.1 Task 0.5 已移除模块

描述：

清理背包系统遗留代码，完成架构从“物品/背包”向“线索/疑点”方向的基座切换，避免旧系统继续参与运行时逻辑。

包含内容：

- 移除 Autoload：`project.godot` 中删除 `PlayerState`。
- 删除脚本：`scripts/characters/player/player_state.gd`。
- 删除脚本：`scripts/objects/item_data.gd`。
- 删除脚本：`scripts/objects/pickable_item.gd`。
- 删除脚本：`scripts/UI/inventory_ui.gd`。
- 删除场景：`scenes/objects/pickable_item.tscn`。
- 删除场景：`scenes/UI/inventory_ui.tscn`。
- 场景清理：`scenes/characters/player/player.tscn` 中移除 `InventoryUI` 实例。
- 场景清理：`scenes/test/test_scene_player.tscn` 中移除 `PickableItem` 实例及相关资源引用。
- 场景清理：`scenes/rooms/room_song_lin_xi.tscn` 中移除 `PickableItem` 实例及对应场景引用。

### 3.2 Task 1.2 场景门/过道配置结果

描述：

执行“过道自动触发、门交互触发”的混合策略，并与开发文档中的清单保持一致。

包含内容：

- 自动触发（Passage）：
  - `scenes/rooms/hall.tscn`：`F2Left`、`F2Right`、`ZouLang`
  - `scenes/rooms/zou_lang.tscn`：`Hall`
  - `scenes/rooms/floor2.tscn`：`HallLeft`、`HallRight`
- 交互触发（Door）：
  - 以上三个场景中的其余门节点。
  - `scenes/rooms/can_ting.tscn`、`scenes/rooms/hui_ke_ting.tscn`、`scenes/rooms/room_song_lin_xi.tscn`、`scenes/rooms/room_wu_ting_xiang.tscn` 的门节点。
- 连接清理：所有 Door 节点已移除 `body_entered -> _on_body_entered` 连接；仅 Passage 节点保留该连接。

### 3.3 Task 2.1 场景管理提取（SceneManager）

描述：

将 `game_root.gd` 中场景管理职责提取到 `SceneManager` 单例，实现切场景职责集中化，并保留当前对话链路在 `game_root.gd` 的过渡实现。

包含内容：

- 对已有内容的变更：`project.godot` 新增 `SceneManager` Autoload 注册（`res://scripts/scenes/scene_manager.gd`）。
- 对已有内容的变更：`scripts/game_root.gd` 移除房间加载/切换/玩家常驻管理逻辑，改为在 `_ready()` 调用 `SceneManager.initialize(...)`；对话结束时通过 `SceneManager.is_transitioning` 判断过场状态。
- 创建了新的内容：`scripts/scenes/scene_manager.gd`，集中实现房间容器管理、常驻玩家管理、出生点放置、过场淡入淡出与切场景信号监听。

### 3.4 Task 2.2 游戏状态与暂停统一入口（GameManager）

描述：

创建 `GameManager` 单例并作为全局状态与暂停的统一入口，接管原本分散在 `game_root.gd` 与 `scene_manager.gd` 的暂停调用。

包含内容：

- 对已有内容的变更：`project.godot` 新增 `GameManager` Autoload 注册（`res://scripts/game_manager.gd`）。
- 对已有内容的变更：`scripts/game_root.gd` 对话开始与结束改为调用 GameManager 状态接口，不再直接写 `get_tree().paused`。
- 对已有内容的变更：`scripts/scenes/scene_manager.gd` 过场切换改为调用 GameManager 的暂停令牌接口（`scene_transition`）。
- 对已有内容的变更：`scripts/UI/main_menu.gd` 主菜单与开始游戏流程同步写入 GameManager 状态。
- 创建了新的内容：`scripts/game_manager.gd`，实现游戏状态机、Esc 暂停入口、对话状态入口、暂停令牌管理与状态变更信号。

### 3.5 Task 2.3 对话入口迁移（DialogueManager）

描述：

新增 `DialogueManager` 单例，迁移 `game_root.gd` 中 Dialogic 启动/结束处理，统一对话防重入与暂停恢复，并为未来 `npc_id + entry_id` 路由模式预留兼容入口。

包含内容：

- 对已有内容的变更：`project.godot` 新增 `DialogueManager` Autoload 注册（`res://scripts/dialogue/dialogue_manager.gd`）。
- 对已有内容的变更：`scripts/event_bus.gd` 中 `dialogue_requested` 信号升级为 `Variant` 载荷，兼容旧 `String` 请求并支持未来 `Dictionary` 请求。
- 对已有内容的变更：`scripts/game_root.gd` 移除对话启动/结束逻辑与对应信号连接，回归启动入口职责。
- 创建了新的内容：`scripts/dialogue/dialogue_manager.gd`，实现对话请求监听、时间线解析、防重入、Dialogic 启停与结束恢复。

### 3.6 Task 2.4 启动入口收敛与旧链路清理

描述：

将 `game_root.gd` 收敛为纯启动入口，并清理确认不再使用的旧信号/旧函数命名与无效常量，减少历史兼容负担。

包含内容：

- 对已有内容的变更：`scripts/game_root.gd` 仅保留启动调用，显式调用 SceneManager、GameManager、DialogueManager。
- 对已有内容的变更：`scripts/event_bus.gd` 删除未使用的旧信号 `change_room_requested`，统一使用 `scene_change_requested`。
- 对已有内容的变更：`scripts/scenes/scene_manager.gd` 移除旧信号连接，并将处理函数命名更新为 `_on_scene_change_requested`。
- 对已有内容的变更：`scripts/UI/main_menu.gd` 删除未使用常量 `FIRST_LEVEL_PATH`。
- 对已有内容的变更：`scripts/dialogue/dialogue_manager.gd` 增加 `bootstrap()` 启动钩子，配合 GameRoot 启动入口调用。

### 3.7 Task 3.1 线索数据 Resource 定义（ClueData）

描述：

按开发文档 Phase 3 Task 3.1 定义 `ClueData` 资源结构，覆盖简化线索字段与深入调查父子关系字段，并同步开发/说明文档进度状态。

包含内容：

- 对已有内容的变更：`开发文档.md` 更新“当前进度标记”为截至 2026-04-05，并标注 `Phase 3：Task 3.1 已完成`。
- 对已有内容的变更：`开发文档.md` 在“已完成（可直接复用）”中新增 `ClueData` 条目，并从“待创建”中移除“线索数据”。
- 对已有内容的变更：`说明文档.md` 完成“2.3.1 线索数据结构”章节，补充 `ClueData` 字段说明与用途定义。
- 创建了新的内容：`scripts/objects/interactable/clue_data.gd`，新增 `class_name ClueData extends Resource` 及 Task 3.1 要求字段。

### 3.8 线索可重复调查与自毁逻辑收敛

描述：

根据“线索可重复调查、线索节点不因调查删除”的规则，修正 Interactable 基类中过于宽泛的自毁条件，避免线索类交互对象被 world_flags 误清理。

包含内容：

- 对已有内容的变更：`scripts/objects/interactable/interactable.gd` 的 `_ready()` 增加 `is_one_time` 前置判断，仅一次性交互对象启用 world_flags 自毁。
- 对已有内容的变更：`开发文档.md` 的“场景重载与世界状态”章节改为“一次性交互记录与自毁”表述，示例从线索改为箱子类对象。
- 对已有内容的变更：`说明文档.md` 的 Interactable 章节同步更新为“仅一次性对象触发自毁”。

### 3.9 Task 3.2 DataManager 线索状态与接口实现

描述：

完成 Phase 3 Task 3.2：在 DataManager 中实现线索运行时状态结构、线索查询/写入接口、父子线索查询接口，以及首次发现与首次深入调查提示信号。

包含内容：

- 对已有内容的变更：`scripts/data_manager.gd` 从骨架升级为线索管理实现，新增 `clue_defs`、`clue_states`、`ui_notice_requested`，并实现 `add_clue()`、`has_clue()`、`get_discovered_clues()`、`get_clues_by_category_path()`、`get_clues_by_tag()`、`get_parent_clue_id()`、`get_child_clue_ids()`、`is_deep_unlocked()`、`mark_deep_unlocked()`、`mark_clue_read()`。
- 对已有内容的变更：`开发文档.md` 更新进度为 `Phase 3：Task 3.1 ~ 3.2 已完成`，并同步资产盘点中 DataManager 状态。
- 对已有内容的变更：`说明文档.md` 的 DataManager 章节改为 Task 3.2 后的接口与字段说明。
- 创建了新的内容：无（本任务为既有文件增量实现）。

### 3.10 Task 3.3 ClueItem 交互流程落地

描述：

完成 Phase 3 Task 3.3：新增 `ClueItem` 交互组件，实现“线索可重复调查、一般线索单条详情、深入线索首轮父/后续父+子”规则，并通过 EventBus 派发交互详情请求。

包含内容：

- 对已有内容的变更：`scripts/event_bus.gd` 新增 `clue_interaction_details_requested(payload: Dictionary)` 信号，作为线索交互详情的统一行为请求入口。
- 对已有内容的变更：`开发文档.md` 更新进度为 `Phase 3：Task 3.1 ~ 3.3 已完成`，并同步接口约定表与资产盘点。
- 对已有内容的变更：`说明文档.md` 新增 `ClueItem` 组件章节，明确交互轮次规则与数据流向。
- 创建了新的内容：`scripts/objects/interactable/clue_item.gd`，实现线索交互、定义注册、深入调查判定与详情事件派发。

### 3.11 Task 3.4（分阶段）雪墅章节线索资源映射

描述：

按用户要求先完成 Task 3.4 的雪墅章节范围：将 `线索.txt` 中雪墅条目映射为可配置 `ClueData` 资源，并补齐该章节涉及的深入调查父子关系。

包含内容：

- 对已有内容的变更：`开发文档.md` 进度新增“Task 3.4 进行中（已完成雪墅章节映射）”，并在“已完成（可直接复用）”中加入雪墅线索资源条目。
- 创建了新的内容：`assets/objects/clues/ch1_snow_villa/` 下新增 38 个 `ClueData` 资源文件（基础线索 32 + 深入调查子线索 6），文件名与线索 id 一致。
- 创建了新的内容：`assets/objects/clues/ch1_snow_villa/mapping_snow_chapter.md`，记录章节范围、统计信息、父子映射表与章节边界说明。

### 3.12 PackedStringArray 全量统一与脚本改动回填

描述：

按“线索数组全部统一为 PackedStringArray”要求，补齐并固化 `DataManager` 与 `ClueData` 相关改动记录，同步修正接口文档，确保资源层、脚本层与文档层一致。

包含内容：

- 对已有内容的变更：`scripts/data_manager.gd` 线索查询接口统一为 `PackedStringArray`（`get_discovered_clues`、`get_clues_by_category_path`、`get_clues_by_tag`、`get_child_clue_ids`），并保留现有业务行为。
- 对已有内容的变更：`scripts/objects/interactable/clue_item.gd` 适配 `PackedStringArray` 子线索读取，向 UI 事件负载继续输出兼容的字符串数组结构。
- 对已有内容的变更：`scripts/objects/interactable/clue_data.gd` 的 `category_path`、`tags`、`child_clue_ids` 统一类型（此前已改）在文档中正式回填。
- 对已有内容的变更：`开发文档.md` 与 `说明文档.md` 中 ClueData 字段定义、DataManager 接口签名同步更新为 `PackedStringArray`。
- 对已有内容的变更：`assets/objects/clues/ch1_snow_villa/1_dining_1.tres`、`assets/objects/clues/ch1_snow_villa/1_dining_2.tres` 补齐显式字段，保证与同目录资源格式一致。

### 3.13 Task 3.4 全章节收尾（梦墅 + 异墅）

描述：

完成 Task 3.4 剩余范围：将 `线索.txt` 的梦墅（chapter 3）与异墅（chapter 4）条目全部映射为 `ClueData` 资源，补齐深入调查父子关系，并将需要附件的线索按规则拼接附件文本。

包含内容：

- 对已有内容的变更：`开发文档.md` 将 Task 3.4 状态从“进行中（仅雪墅）”更新为“已完成（雪墅/梦墅/异墅全章节）”。
- 创建了新的内容：`assets/objects/clues/ch2_dream_villa/`，新增梦墅章节线索资源（含深入调查子线索）与 `mapping_dream_chapter.md` 映射文档。
- 创建了新的内容：`assets/objects/clues/ch3_strange_villa/`，新增异墅章节线索资源（含深入调查子线索）与 `mapping_strange_chapter.md` 映射文档。
- 对已有内容的变更：为所有新增父线索写入 `child_clue_ids`，并为每个深入调查子线索写入 `parent_clue_id`，实现可被 UI 直接读取的父子关系。
- 对已有内容的变更：对“见附件N”的线索条目按“空一行拼接附件内容”规则写入 `description` 文本。
- 对已有内容的变更：本次新增资源统一采用 `PackedStringArray` 与 UTF-8 无 BOM 写入格式，保持与现有雪墅资源一致。

### 3.14 Task 5.1 线索栏 UI（提前执行）

描述：

按开发顺序调整先行执行 Phase 5 Task 5.1，完成线索栏 UI 的基础可用版本：支持 `archive/interaction` 双模式、目录树分层浏览、搜索过滤、交互详情展示，以及打开面板时背景轻度变暗。

包含内容：

- 对已有内容的变更：`scenes/game_root.tscn` 挂载 `CluePanel`，使线索栏在游戏主流程中可用。
- 对已有内容的变更：`开发文档.md` 更新进度与资产盘点，标记 `Phase 5 Task 5.1` 已完成并从“待创建”迁出。
- 对已有内容的变更：`说明文档.md` 新增 ClueUI 模块说明（本条目）。
- 创建了新的内容：`scripts/UI/clue_ui.gd`，实现线索栏模式切换、目录构建、筛选、详情展示、交互详情接入与暂停令牌管理。
- 创建了新的内容：`scenes/UI/clue_panel.tscn`，实现线索栏可视层级（Dimmer + 1280x800 居中面板 + 左目录右详情布局）。

### 3.15 Task 5.1 联调修复与体验增强

描述：

基于测试场景回归与用户反馈，完成线索手册联调收敛：补齐测试入口、修复点击崩溃与深入子线索入档问题，并落实归档层级/排序/选中保持等体验需求，同时处理脚本兼容与编码问题。

包含内容：

- 对已有内容的变更：`scripts/UI/clue_ui.gd` 增加 deferred 刷新与空值防御，修复目录刷新重入导致的空引用崩溃；归档展示改为父子层级；新增排序模式（默认固定顺序/按发现时间）；修复点击后跳首条问题；兼容 Godot 对嵌套类型集合的限制。
- 对已有内容的变更：`scenes/UI/clue_panel.tscn` 新增排序下拉控件，接入线索列表排序切换。
- 对已有内容的变更：`scripts/data_manager.gd` 在 `mark_deep_unlocked()` 中补充子线索发现登记（写入 `clue_states` 与 `discover_order`），保证深入调查后子线索进入手册。
- 对已有内容的变更：`scenes/test/test_scene_player.tscn` 增加多组测试线索（普通、可深入搜身、第四章）并挂载 `CluePanel`，用于线索手册回归验证。
- 对已有内容的变更：`scripts/UI/main_menu.gd` 将“选项”按钮绑定到测试场景，形成稳定复测入口。
- 对已有内容的变更：`test_map_rect.gd` 统一为 UTF-8 编码（无 BOM），修复脚本解析阶段的 UTF-8 错误告警。
- 对已有内容的变更：`开发文档.md` 回填 Task 5.1 联调后的行为约束与验收口径（层级、排序、选中保持、稳定性）。
- 创建了新的内容：无（本轮为既有模块增量修复与联调优化）。

### 3.16 线索人名、人物 ID 与章节编号同步替换

描述：

完成线索数据中的人物名称替换，并同步涉及人物姓氏与章节编号的线索资源 id、文件名与父子引用关系。

包含内容：

- 对已有内容的变更：`assets/objects/clues/` 全量替换 `松林夕 -> 周崇安`、`穆睽信/穆暌信 -> 穆执`、`苏溟玖 -> 林玖`、`夏中清 -> 钟歧`。
- 对已有内容的变更：按新姓氏更新线索 ID 与文件名：`*_song_* -> *_zhou_*`、`*_su_* -> *_lin_*`、`*_xia_* -> *_zhong_*`；`mu` 因姓氏不变保留。
- 对已有内容的变更：修正章节编号偏移，线索 ID、文件名、`chapter_id` 与 `chapter_*` 标签统一从雪墅/梦墅/异墅对应 `2/3/4` 改为 `1/2/3`。
- 对已有内容的变更：同步重命名章节资源目录：`ch2_snow_villa -> ch1_snow_villa`、`ch3_dream_villa -> ch2_dream_villa`、`ch4_strange_villa -> ch3_strange_villa`。
- 对已有内容的变更：同步更新 `ClueData` 内部 `id`、`room_id`、`parent_clue_id`、`child_clue_ids`、`category_path` 以及 mapping 文档中的父子映射。
- 对已有内容的变更：`线索.txt` 同步替换人名与对应线索 ID 示例/room_id 说明。
- 对已有内容的变更：`scenes/test/test_scene_player.tscn` 更新测试线索资源路径与 `clue_id/interact_id`，避免旧资源路径失效。

### 3.17 对话线索自动注册补齐

描述：

为解决 Dialogic 时间线直接调用 `DataManager.add_clue()` 时，线索手册可能缺失 `ClueData` 静态定义的问题，在 `DataManager` Autoload 初始化阶段补上全量 `ClueData` 自动扫描注册流程，并新增未注册线索 ID 的运行时警告。

包含内容：

- 对已有内容的变更：`scripts/data_manager.gd` 新增启动时递归扫描 `res://assets/objects/clues/` 的自动注册逻辑，将全部 `.tres` 线索资源写入 `clue_defs`，从而保证对话获得的搜身线索、重复搜身子线索等也能被 `ClueUI` 正确读取父子关系。
- 对已有内容的变更：`scripts/data_manager.gd` 的 `add_clue()` 在目标 `clue_id` 未注册静态定义时输出警告，便于排查资源漏配置或新增线索未入库的问题。
- 对已有内容的变更：`说明文档.md` 的 DataManager 章节补充自动注册与告警行为说明。
- 创建了新的内容：无（本轮为既有模块增量修复）。

### 3.18 雪墅 1.1-1.3 对话与口供线索落地

描述：

根据 `对话.md` 补齐雪墅章节前期 Dialogic 时间线与口供线索资源，覆盖人物介绍、最初搜证、搜身、最初推理等流程。对话中涉及“记录疑点”“推理系统”的位置暂不接入系统逻辑，仅保留台词与叙事内容。

包含内容：

- 创建了新的内容：`assets/dialogues/ch1_snow_villa/1_1_wu.dtl`、`1_1_zhong.dtl`，补齐乌停湘与钟歧人物介绍时间线。
- 创建了新的内容：`assets/dialogues/ch1_snow_villa/1_1_butler_a.dtl`、`1_2_butler_a.dtl`、`1_2_butler_b.dtl`、`1_2_butler_c.dtl`、`1_2_butler_d.dtl`，补齐管家引导搜证、案发现场前后与调查结束入口。
- 对已有内容的变更：`1_1_lin.dtl`、`1_1_mu.dtl`、`1_1_zhou.dtl`、`1_1_wu.dtl`、`1_1_zhong.dtl` 增加搜身与再次搜身分支；首次搜身收录对应搜身线索，第二次坚持搜身扣除好感度，并按需要收录深入线索。
- 创建了新的内容：`assets/objects/clues/ch1_snow_villa/1_testimony_1_butler.tres` 至 `1_testimony_6_lin.tres`，新增雪墅 1.3 所需口供线索；其中 `1_testimony_6_lin` 挂载为 `1_testimony_4_all` 的子线索。
- 创建了新的内容：`assets/dialogues/ch1_snow_villa/1_3_butler_lin_mu_wu_zhong_zhou.dtl`，实现 1.3 最初推理多人时间线，按剧情节点调用 `DataManager.add_clue()` 收录口供线索。
- 对已有内容的变更：对本轮涉及的对话与线索文本执行旧名到新名替换，统一使用 `钟歧`、`林玖`、`穆执`、`周崇安` 等新版角色名。

### 3.19 Phase 4 / Phase 5 疑点与推理系统落地

描述：

完成疑点数据、推理校验、推理手册 UI 与线索手册选择联动的首轮落地，并将第一章对话中的疑点触发正式接入运行时系统。

包含内容：

- 对已有内容的变更：`scripts/event_bus.gd` 新增 `clue_selection_requested`、`clue_selected_for_reasoning`、`ui_panel_focus_requested`，用于手册切换与推理选线索联动。
- 对已有内容的变更：`scripts/data_manager.gd` 新增 `suspicion_defs` 自动注册、疑点运行时状态、`add_suspicion()`、`get_all_suspicions()`、`mark_suspicion_read()`、`resolve_suspicion()` 等接口，并在 `_ready()` 中递归扫描 `assets/objects/suspicions/`。
- 创建了新的内容：`scripts/logic/suspicion_data.gd`，定义 `SuspicionData` Resource，包含 `chapter_id`、`category_path`、`tags`、`required_clue_ids: PackedStringArray`、`match_mode` 与 `conclusion_clue_id`。
- 创建了新的内容：`scripts/logic/reasoning_system.gd`，统一实现疑点校验逻辑。
- 对已有内容的变更：`scripts/UI/clue_ui.gd` 从双模式扩展为三模式，新增 `selection` 模式，并在右侧详情底部提供“选择线索”按钮。
- 创建了新的内容：`scripts/UI/suspicion_ui.gd` 与 `scenes/UI/suspicion_panel.tscn`，实现“推理手册”界面、疑点树、红点、搜索排序、3 槽位推理与结论展示。
- 对已有内容的变更：`scenes/game_root.tscn` 挂载 `SuspicionPanel`，并通过 `ui_panel_focus_requested` 实现与 `CluePanel` 的单层互斥切换。
- 创建了新的内容：`assets/objects/suspicions/ch1_snow_villa/` 下第一章疑点资源，以及对应的第一章结论线索资源。
- 对已有内容的变更：第一章相关 `.dtl` 时间线改为在对应剧情节点调用 `DataManager.add_suspicion()`；同时修正部分口供线索引用错误并补齐漏收录的对话线索。
- 对已有内容的变更：`开发文档.md` 与 `说明文档.md` 同步更新为当前实际实现，而非此前的规划状态。
